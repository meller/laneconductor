# Track AM-10096: Nested .claude/.claude/... directory tree keeps growing one level deeper on every track worktree merge, now 5 levels deep on main

**Lane**: implement
**Lane Status**: running
**Progress**: 100%
**Phase**: Planned — 7 phases, root cause confirmed
**Type**: dev
**Merge Mode**: direct
**Auto Run**: yes
**Author**: AM
**Created By**: 2565050+meller@users.noreply.github.com
**Problem**: Confirmed by direct inspection, not from the reported symptom alone: the primary checkout currently has a REAL, physically-materialized (not symlinked) directory tree at .claude/.claude/.claude/.claude/.claude/ -- 5 levels deep, 480 files total under .claude/.claude/ (6.4MB), containing full duplicated copies of every marketing/dev skill (ab-test-setup, ad-creative, social-content, etc.) plus nested MEMORY.md and settings.json files. This is NOT new: comparing git ls-tree across commits shows the depth was ALREADY at 4 levels (.claude/.claude/.claude/.claude/, 360 files at the 3-deep prefix) in the commit immediately before AM-10093's merge (cb563fe6) -- the merge (2eb37528, "Merge track 10093") did not create this corruption, it just added the 5th level (120 more files) on top of pre-existing rot, and its own diff shows 135 files changed / 29437 insertions, of which only ~10 files (~600 lines) were the tracks real fix (conductor/services/dispatch-revalidation.mjs, db-pull-guard.mjs, orphan-worker-detection.mjs, suppression-log-throttle.mjs, run-marker.mjs, laneconductor.sync.mjs, and their tests) -- the remaining ~28,800 lines were this nested skills-directory bloat riding along. Root cause not yet confirmed but the pattern (one new nesting level per track lifecycle) strongly suggests the setup/scaffold logic that is supposed to symlink .claude/skills/laneconductor from the canonical install path (per SKILL.md's own "Installation" section: ln -sf "$SKILL_DIR" "$TARGET") is instead, in some code path (possibly triggered inside a track worktree, where the skill directory resolution differs from the primary checkout), copying/materializing the ENTIRE parent .claude directory as a real subdirectory of itself rather than creating a symlink, and this keeps recursing across track worktree lifecycles (create worktree -> corrupted copy appears -> worktree merges back -> corruption lands on main -> next track's worktree copies THAT corrupted tree too, adding one more level). Needed: (1) find the exact code path that materializes .claude as a nested copy instead of symlinking it -- likely in the worktree-creation logic (conductor/services/worktree-merge.mjs or the lock/worktree creation in laneconductor.sync.mjs) or in a setup-scaffold step that runs unexpectedly inside a worktree context; (2) fix it to never recurse past one real .claude directory; (3) decide on and execute a safe cleanup of the current 5-deep nest on main and in any other worktrees/branches that carry it, distinguishing genuinely-needed files (if any were added at a deeper level that do not exist at the top level) from pure duplication before deleting anything, per this session's own standing instruction to investigate rather than blindly delete unfamiliar nested state.
**Summary**: Confirmed by direct inspection, not from the reported symptom alone: the primary checkout currently has a REAL, physically-materialized (not symlinked) directory tree at .claude/.claude/.claude/.claude/.claude/ -- 5 levels deep, 480 files total under .claude/.claude/ (6.4MB), containing full duplicated copies of every marketing/dev skill (ab-test-setup, ad-creative, social-content, etc.) plus nested MEMORY.md and settings.json files. This is NOT new: comparing git ls-tree across commits shows the depth was ALREADY at 4 levels (.claude/.claude/.claude/.claude/, 360 files at the 3-deep prefix) in the commit immediately before AM-10093's merge (cb563fe6) -- the merge (2eb37528, "Merge track 10093") did not create this corruption, it just added the 5th level (120 more files) on top of pre-existing rot, and its own diff shows 135 files changed / 29437 insertions, of which only ~10 files (~600 lines) were the tracks real fix (conductor/services/dispatch-revalidation.mjs, db-pull-guard.mjs, orphan-worker-detection.mjs, suppression-log-throttle.mjs, run-marker.mjs, laneconductor.sync.mjs, and their tests) -- the remaining ~28,800 lines were this nested skills-directory bloat riding along. Root cause not yet confirmed but the pattern (one new nesting level per track lifecycle) strongly suggests the setup/scaffold logic that is supposed to symlink .claude/skills/laneconductor from the canonical install path (per SKILL.md's own "Installation" section: ln -sf "$SKILL_DIR" "$TARGET") is instead, in some code path (possibly triggered inside a track worktree, where the skill directory resolution differs from the primary checkout), copying/materializing the ENTIRE parent .claude directory as a real subdirectory of itself rather than creating a symlink, and this keeps recursing across track worktree lifecycles (create worktree -> corrupted copy appears -> worktree merges back -> corruption lands on main -> next track's worktree copies THAT corrupted tree too, adding one more level). Needed: (1) find the exact code path that materializes .claude as a nested copy instead of symlinking it -- likely in the worktree-creation logic (conductor/services/worktree-merge.mjs or the lock/worktree creation in laneconductor.sync.mjs) or in a setup-scaffold step that runs unexpectedly inside a worktree context; (2) fix it to never recurse past one real .claude directory; (3) decide on and execute a safe cleanup of the current 5-deep nest on main and in any other worktrees/branches that carry it, distinguishing genuinely-needed files (if any were added at a deeper level that do not exist at the top level) from pure duplication before deleting anything, per this session's own standing instruction to investigate rather than blindly delete unfamiliar nested state.
