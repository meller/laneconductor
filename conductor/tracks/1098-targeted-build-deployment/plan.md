# Track 1098 Plan: Targeted Build Deployment UI & Release Notes Preview

## Implementation Tasks

- [ ] Task 1: Update `WorkersList.jsx` deployment drawer to support selecting between HEAD and Build Artifacts.
- [ ] Task 2: Build AI Release Notes preview panel inside the deployment drawer.
- [ ] Task 3: Update `POST /api/projects/:id/deploy` server route to process `buildId` and inject build env vars.
- [ ] Task 4: Update `DeploymentsPage.jsx` audit log view to display `buildId` badges.
- [ ] Task 5: Write unit & API tests in `ui/server/tests/track-1098-build-deploy-ui.test.mjs`.
