# Track AM-10097: Canonical project scaffold gitignore template is missing several disposable runtime-state patterns this repo already carries

**Lane**: plan
**Lane Status**: running
**Progress**: 0%
**Phase**: New
**Type**: dev
**Merge Mode**: direct
**Auto Run**: yes
**Author**: AM
**Created By**: 2565050+meller@users.noreply.github.com
**Problem**: Confirmed by direct comparison, not from the reported symptom alone: this repo own .gitignore already excludes several categories of pure DB/API-synced or per-machine runtime state that a NEW project scaffolded via lc setup (bin/lc.mjs) or the skill-only scaffold path (SKILL.md, no CLI) never receives, because the shared templates in those two places were never kept in sync with what this repo learned it needed the hard way. Confirmed gaps, cross-checked against this repo own .gitignore and the exact source files that write each one: (1) .conv-cursor (per-track sync cursor, written by conductor/laneconductor.sync.mjs -- already fixed this session in both bin/lc.mjs setup and SKILL.md scaffold); (2) .worktrees/ (git worktree checkouts -- already gitignored here at .gitignore:28, but absent from both scaffold templates); (3) conductor/.runs/ (per-run dispatch markers -- already gitignored here at .gitignore:35, absent from both templates); (4) conductor/tracks/**/.prespawn-block-count and conductor/tracks/**/.prespawn-block-kind (per-track counters written by conductor/services/prespawn-block-counter.mjs, COUNT_FILE/KIND_FILE constants -- not gitignored ANYWHERE, including in this repo own .gitignore, despite being the exact same disposable-bookkeeping category as .conv-cursor). Discovered while investigating a real incident in the macrodash project (a sibling LaneConductor-managed repo, not this one): its own dirty checkout had accumulated 6 track worktrees accidentally tracked as git submodule-style gitlinks (009, 027, 030, 031, 26, 29) sitting alongside 30+ correctly-untracked worktree directories for every other active track -- strong evidence the 6 got `git add`-ed by accident at some point (this repos own memory notes document a related "never git add -A in a worktree" hazard), not that worktrees are sometimes meant to be tracked. Also found in macrodash: conductor/.runs/083.json and a tracks .prespawn-block-count/.prespawn-block-kind pair sitting dirty for the same reason. Separately, and NOT yet understood: macrodash also has a single orphaned file at .agents/tracks/071-simplify-auth-pages/plan.md, under a directory structure (.agents/tracks/) that does not exist anywhere in this repo (grepped bin/lc.mjs, conductor/laneconductor.sync.mjs, and SKILL.md -- zero references) -- needs investigation before any gitignore decision, since unlike the other findings this might be real, intentionally-placed content under an unfamiliar structure rather than sync noise. Needed: (1) add .worktrees/, conductor/.runs/, conductor/tracks/**/.prespawn-block-count, and conductor/tracks/**/.prespawn-block-kind to both scaffold templates (bin/lc.mjs setup command and SKILL.md setup scaffold section), same treatment as the already-fixed .conv-cursor; (2) add conductor/tracks/**/.prespawn-block-count and .prespawn-block-kind to this repos OWN .gitignore too, since it is currently missing even here; (3) decide on and, if approved, execute the macrodash-specific cleanup -- gitignore .worktrees/ there and untrack the 6 accidental gitlinks (git rm --cached, not a working-tree delete, since the worktrees themselves must keep working) -- this is a bigger architectural call than the other fixes (removing tracked gitlinks) so get explicit confirmation before touching macrodash directly; (4) investigate .agents/tracks/071-simplify-auth-pages/plan.md in macrodash before deciding whether it is genuine content, a stray copy, or another symlink-resolution artifact like the AM-10096 nested .claude corruption.
