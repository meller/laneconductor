# Tests: Track 1063 — Setup Simplification

## Test Commands
```bash
# Create dummy project
mkdir -p /tmp/lc-test-setup
cd /tmp/lc-test-setup
# Run lc setup (simulated input if possible, or manual)
# We can use a script to feed inputs to lc setup
```

## Test Cases
- [ ] TC-1: `lc setup` manual phase works.
- [ ] TC-2: `lc setup` automatically spawns AI agent.
- [ ] TC-3: `lc setup` generates context files correctly.
- [ ] TC-4: `lc setup` reports success or failure of AI part.
