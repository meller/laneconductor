# Track AM-10087: Blocked-turn park override discards a lane action's own already-resolved verdict

**Lane**: quality-gate
**Lane Status**: running
**Progress**: 100%
**Phase**: New
**Type**: dev
**Merge Mode**: direct
**Auto Run**: yes
**Author**: AM
**Created By**: 2565050+meller@users.noreply.github.com
**Problem**: The sync worker's post-turn processing (conductor/laneconductor.sync.mjs, the isBlockedTurn/agentReportedWaiting park logic around line 6494) forces ANY turn whose LAST post_turn_summary event has status_category:'blocked' into lane_action_status 'waiting' — staying in the SAME lane — with no check for whether the lane action itself already produced a definitive, mechanically-resolvable outcome that workflow.json's own transition table already knows how to route. Confirmed live: livingwork's AM-1018 review action delivered a complete, unambiguous FAIL verdict in its main closing response ('Review complete. The track is back in implement at 85%. Verdict: FAIL.' plus 1 blocker + 6 detailed findings) — workflow.json's own table routes review's on_failure straight to implement:queue, no human judgment involved, that is the entire point of an automated review gate. But the SAME turn's underlying Claude Code harness separately emitted a post_turn_summary with status_category:'blocked' and needs_action:'decide: fix before merge or defer to Phase 7?' — a question whose answer is already fixed by the project's own workflow policy (there is no 'defer' lane transition; FAIL always returns to implement). isBlockedTurn's override saw that one harness-level annotation, ignored the review's own already-computed verdict, and parked the track at review:waiting with lane unchanged and (worse) no Waiting Reason marker written at all — a park-without-a-reason case the code's own comments (Track 10055/REQ-3/REQ-14) flag as 'the protocol being ignored, not a normal outcome,' yet nothing currently catches or surfaces that. The track sat stuck indefinitely despite Auto Run: yes, because a parked track is invisible to the auto-launch queue scan (same root architecture as AM-10086, but here the underlying transition was ALREADY fully resolved by the action itself — this isn't even a case that needs AM-10086's dependency-clearing reconciliation, it should never have parked in the first place). Manually corrected via PATCH /api/projects/:id/tracks/:num {lane_status:'implement'} to unblock. Requested: isSuccess (currently just `code === 0`, the CLI process's own exit status, unrelated to the lane action's semantic verdict) needs a real path to learn a review/quality-gate action's own PASS/FAIL verdict, and the isBlockedTurn park override must not fire when that verdict is already resolved and mechanically routable per workflow.json — a genuine open question (e.g. 'should I apply this DB migration?') should still park, but a review's own complete FAIL-with-findings output should not be treated as one just because the harness's own end-of-turn self-assessment happened to tag it 'blocked'. Open question for planning: whether to parse the verdict from the action's own closing text (fragile, wording-dependent) or have the review/quality-gate skill instructions write a structured, unambiguous marker (e.g. **Verdict**: pass|fail) the sync worker can check deterministically before ever consulting isBlockedTurn.
**Summary**: The sync worker's post-turn processing (conductor/laneconductor.sync.mjs, the isBlockedTurn/agentReportedWaiting park logic around line 6494) forces ANY turn whose LAST post_turn_summary event has…
**Waiting for reply**: no
