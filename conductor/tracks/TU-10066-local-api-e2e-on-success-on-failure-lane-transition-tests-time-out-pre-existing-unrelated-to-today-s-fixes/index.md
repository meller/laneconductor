# Track TU-10066: local-api-e2e: on_success/on_failure lane transition tests time out (pre-existing, unrelated to today's fixes)

**Lane**: implement
**Lane Status**: success
**Progress**: 0%
**Phase**: New
**Type**: dev
**Author**: TU
**Created By**: test@example.com
**Problem**: conductor/tests/local-api-e2e.test.mjs's 'on_success: implement -> review' and 'on_failure: quality-gate exhausts retries -> failure status' subtests both time out after 20s polling for a lane transition that never happens. Confirmed PRE-EXISTING and unrelated to any of today's changes via direct A/B: swapped conductor/services/worktree-artifact-merge.mjs between its pre-fix and post-fix versions (the track 10065/skipUnchanged fix) with everything else held constant, and both versions produce the identical two failures. Also ruled out conductor/tests/mock-collector.mjs's missing routes as the cause: track 10051's rename commit (42c30f9) deleted this file while it was still used by 48 test files, and restoring it surfaced several UNHANDLED routes (POST /track/:num/prespawn-block and its /reset, GET /track/:num, GET /project/:id/dispatch/claimed-by-offline-workers, POST /_set-offline-workers) that got added back in the same session (see track 10065's fix commit), but adding every one of them and confirming zero UNHANDLED lines remain in the test output did not change the outcome for these two subtests - they still time out identically. Whatever is actually wrong is upstream of both of those, in the real on_success/on_failure lane-transition logic itself or in some other mock gap not yet found. Scope for planning: bisect further, ideally by adding targeted logging around the actual on_success/on_failure dispatch path in conductor/laneconductor.sync.mjs to see exactly where the two test runs stall, or compare against the last known-good run of this specific test file to find when it actually broke (git log -- conductor/tests/local-api-e2e.test.mjs and separately check when the underlying on_success/on_failure code paths last changed) rather than continuing to guess. Not urgent - this is a pre-existing gap in test coverage for a feature that appears to work correctly in live use throughout today's session (multiple tracks progressed through implement to review and through quality-gate retry exhaustion to failure via the real worker in production, e.g. track 10060's documented retry history), so the underlying feature is not obviously broken - most likely the test's mock setup has drifted from what the real code now expects.
**Summary**: conductor/tests/local-api-e2e.test.mjs's 'on_success: implement -> review' and 'on_failure: quality-gate exhausts retries -> failure status' subtests both time out after 20s polling for a lane…
**Merge Mode**: direct
