{
  "defaultEnvironment": "production",
  "environments": {
    "production": {
      "command": "bash scripts/deploy.sh production",
      "description": "Production AWS/Cloud stack"
    },
    "staging": {
      "command": "bash scripts/deploy.sh staging",
      "description": "Staging preview environment"
    }
  }
}
